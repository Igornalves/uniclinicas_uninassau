import styled from "styled-components";

export const DivConteinerPrincipal = styled.div`
    display: grid;
    grid-template-columns: 332px auto; /* menu lateral + conteúdo */
    grid-template-rows: 140px auto; /* menu cima + conteúdo */
    width: 100vw;
    height: 100vh;
    overflow: hidden;
`
